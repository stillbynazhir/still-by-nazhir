import GalleryClient from "./GalleryClient";

export const metadata = {
  title: "Gallery | Still By Nazhir",
  description: "Browse portraits, weddings, events, and landscape photography by Still By Nazhir.",
};

export default function GalleryPage() {
  return <GalleryClient />;
}
